services:
  api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./src:/app/src
      - ./assets:/app/assets
    command: uvicorn petrus.main:app --host 0.0.0.0 --port 8000 --reload
